# Copy this file to `.env` and fill in real values, then export them into
# your shell (or use a tool like `direnv` / `export $(cat .env | xargs)`)
# before running `mvn spring-boot:run` - Spring reads these as environment
# variables via application.yml (${OPENAI_API_KEY}, ${SERPER_API_KEY}).

# OpenAI - https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-fb3069cc23ac1632-7b9b06-355007be

# Serper.dev - https://serper.dev (used by the Trend Researcher agent)
SERPER_API_KEY=87575a17460acb5e935d82dffa4bec44a984a7d8

# Optional: override the default model (gpt-4o)
OPENAI_MODEL_NAME=gpt-4o
